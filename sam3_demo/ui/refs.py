"""Lightweight component-reference containers used by UI builders."""

from types import SimpleNamespace


class ComponentRefs(SimpleNamespace):
    """Named Gradio component references returned by a tab builder."""
