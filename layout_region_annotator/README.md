# Layout Region Annotator

Standalone Gradio Custom Component for drawing a freehand lasso in source layout-image coordinates.
It intentionally does not provide layout transform controls and never sends server-owned Region geometry back to Python.
